// libraries
import { StrictMode } from 'react';
import ReactDOM from 'react-dom';

// components
import App from './app';

ReactDOM.render(
  <StrictMode>
      <App />
  </StrictMode>,
  document.getElementById('root')
);
